# v3 → v4 优化记录（2026-09-11，仅离线验证）

## 改动（全部证书安全，31-seed 离线配对 + 真源零误排断言通过）

1. `certificate_geometry.py`：新增 `Certificates.potential(ch, point)` —— 严格证书贡献度
   （该站 1000m 盘内仍剩余的整格数，无 ranking guard）。`potential==0` 的站对任何
   未清频道的证书都不可能再作贡献（Q3 整格排除 / Q4 公共站方向覆盖都要求站在某
   剩余格 1000m 内），访问它是可证明的浪费移动，可从路线中丢弃。
2. `practice_robot_v4.py`（v4 = v3 + 下面两项）：
   - 路线任务生成时用 `useful_sites` 过滤扫描站（跨频道复用一次 whole_disk）；
   - Q3：把未检测未清频道的最近"剩余可行格"作为路线任务（`remain`），
     一次无信号即排除一个 1000m 盘，直击证书收尾，尾段确认 122→49s；
   - Q4：保留 v3 定点扫描+补测路径（逐剩余格在 Q4 上过慢且单测少排除，弃用）。

## 31-seed 配对（seed 0-29 + boundary；同场景同误差）

| 问题 | 指标 | v3 | v4 | Δ |
|---|---|---:|---:|---:|
| Q3 | mean 虚拟时间 | 5295s | 4776s | -9.8% |
| Q3 | 尾段确认 mean | 122s | 49s | -60% |
| Q3 | 检测次数 mean | 277 | 184 | -34% |
| Q3 | v4 胜局 | — | 26/31 | — |
| Q4 | mean 虚拟时间 | 10584s | 9815s | -7.3% |
| Q4 | 尾段确认 mean | 802s | 763s | -5% |
| Q4 | 检测次数 mean | 498 | 369 | -26% |
| Q4 | v4 胜局 | — | 27/31 | — |

全部 31/31 完成清除、零事故；离线 harness 逐测断言真实源永不被证书排除、覆盖完整、
计时对账误差 <0.001s。

## 结论与下一动作

v4 作为演练候选版本。下一动作：在模拟器演练环境跑问题3、问题4各若干局（仅演练，
不碰正式测试），把 v4 与 v2/v3 的真实演练成绩分开记录（案例不同不能直接横向比较）。
