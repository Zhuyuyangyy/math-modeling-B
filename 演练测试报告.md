# B题问题3与问题4演练测试报告

2026年9月11日，在用户指定的 Jammers Simulator v1.1 中完成4次真实演练：问题3与问题4各2次，全部清除。本次没有启动任何正式测试。测试结果来自模拟器接口响应及每次演练结束时界面公布的干扰源总数，不是离线自建场景的成绩。

## 真实演练结果

平均定位清除时间使用题目口径：整局虚拟耗时除以成功清除数量，包含最终确认无遗漏的搜索时间。

| 问题 | 版本 | 演练案例编码 | 全向/定向 | 清除/总数 | 清除率 | 总虚拟时间 秒 | 平均定位清除时间 秒/个 | 接口时间戳间隔 秒 |
|---|---|---|---|---|---|---:|---:|---:|
| 3 | v1 基线 | FB4D-US62-JKTD-YMC3 | 12/0 | 12/12 | 100% | 9428.366 | 785.697 | 3.201 |
| 3 | v2 调整覆盖点 | FZT4-JRQM-SAX5-Q23U | 16/0 | 16/16 | 100% | 6730.349 | 420.647 | 1.781 |
| 4 | v1 基线 | 5C4E-SB5T-DVXH-ZDGG | 4/10 | 14/14 | 100% | 12780.368 | 912.883 | 4.681 |
| 4 | v2 调整覆盖点 | XWMN-PJ57-TBRX-UD63 | 0/12 | 12/12 | 100% | 10914.629 | 909.552 | 4.830 |

“接口时间戳间隔”是模拟器 `/exit` 与 `/enter` 响应中的 `real_timestamp_ms` 之差，未把本地脚本计时冒充为界面精确统计的“程序运行时间”。这4次均为演练结果，不可填入论文要求的正式测试表。

四局共清除54/54个目标。样本量只有4局，而且各局案例不同，不能据此宣称普遍成功率100%、策略最优或某个固定百分比的提速。

## 目前表现

问题3已经跑通全向目标的搜索、交会定位和清除。v2在本次16目标案例中的平均耗时约7.01分钟/个。问题4在混合目标和全部定向目标两类实际案例中均完成清除，v2本次平均约15.16分钟/个。

主要耗时来自移动和末尾覆盖搜索。问题3 v2移动约29.81公里；问题4 v2移动约43.52公里。机器狗不知道干扰源总数，因此不能在清除10个或连续若干点无信号时直接结束。当前程序采取完整覆盖检查，代价偏保守，但能解释退出依据。当前定位点选择仍是启发式，不是已经证明的最短时间策略。

## 算法与覆盖依据

1. 每个覆盖点检测尚未清除的频道，先完成该点扫描，再按估计目标距离选择定位次序。同一频道最多一个干扰源，成功清除后永久跳过。
2. 每次示向度构造角度误差扇区，求历史扇区与1500米接收距离上界的交集。使用1.0051度余量覆盖1度误差和读数保留两位小数的舍入。1500米圆采用外接64边形，保持保守包含。初始区域采用包含目标圆的正方形，可能增加工作量，但不排除真实目标。
3. 不确定区域较大时增加有横向基线的检测。区域所有顶点距候选点不超过19米时清除；额外允许一次有记录的试探清除。无信号不直接当作目标不存在。
4. 若有限次数的交会检测仍不足，使用25米方格对剩余可行多边形逐格作光学清除。任意方格内点至格心距离不超过25/√2≈17.68米，小于20米。这是有限的保守后备步骤。
5. 所有覆盖点扫描完成且发现的目标均已清除后退出；若已经清除16个，也可依据题目的总数上限提前退出。出现异常时记录错误并尝试正常结束，不能把异常退出当成成功。

问题3 v1使用21个方格覆盖点，v2改为原点和半径1400米正六边形的6个顶点。半径1000米内由原点覆盖；外环任意目标与最近六边形顶点夹角不超过30度，在半径1000至1800米内最大距离小于915米，所以1000米最小有效接收半径足以覆盖。

问题4 v1使用49个650米方格顶点。v2取边长950米等边三角网格中，所有与目标圆相交三角形的顶点，共31个。任意目标落在某个三角形中，距其三个顶点均不超过950米。通过目标的任一闭半平面至少包含其中一个顶点，因此对任意180度定向发射方向，至少有一个顶点可接收信号。需要保留圆域外的检测点，否则边缘朝外发射的目标可能漏检；题目明确允许机器狗到目标圆域外检测。

几何覆盖论证只依赖公开题设；程序未读取模拟器数据库、目标坐标或其他未公开案例数据。结束界面公布的总数只用于事后核对，不输入搜索算法。

## 文件与核验

- `practice_robot.py`：v2演练程序。
- `practice_robot_v1.py`：基线策略，事后同样补上演练界面保护。
- `results/*.jsonl`：4局完整请求与反馈，队号已替换为占位符。
- `results/*_summary.json`：各局统计及界面真值核对。
- `results/practice_summary.csv`：可直接读取的汇总。
- `results/checksums.sha256`：代码与结果文件校验值。
- `evidence/`：结束界面文字和首轮问题3的完成弹窗截图。

已逐条检查4局请求均为HTTP 200且accepted=true，首尾为enter/exit，成功清除数与界面总数一致。按移动距离/5、检测5秒、切换1秒、清除成功5秒、失败3秒重新核算，四局与模拟器总时间差均小于0.001秒。

另有24个离线自建场景用于初版逻辑检查，以及新版每问题20000个内部/边界点与朝向抽样用于覆盖布局检查；这些验证不计入上表。

## 再次演练的操作

软件中只选择“开始问题3演练测试”或“开始问题4演练测试”，等到“等待机器狗进入”。将队号通过环境变量 `JAMMERS_ROBOT_ID` 提供，运行示例：

```powershell
$env:JAMMERS_ROBOT_ID = '<当前登录队号>'
python practice_robot.py --problem 3 --mode practice --label 'q3_<界面案例编码>'
```

本机可使用Codex捆绑Python。界面校验依赖本机PeekabooWin CLI，可通过环境变量 `PEEKABOO_CLI` 配置路径。代码只接受practice模式，并核对可见界面的问题编号、演练标识、等待进入状态和案例编码；校验失败在任何机器人请求发送前退出。已实测结束状态下会拒绝运行。程序本身没有开始演练或正式测试的接口调用；开始演练需通过明确的演练UI入口。

下一步可在继续只做演练的前提下，增加样本数，研究可行区域驱动的覆盖点选择、剩余频道排除与整体路径排序，降低完整覆盖的移动代价。正式测试不在本次授权范围内。

## v4 演练追加（2026-09-11）

优化（详见 `优化记录_v4.md`）后在真实模拟器演练问题3一局（v4 = 证书过滤扫描站 + Q3 剩余可行格入路线）。

| 问题 | 版本 | 演练案例编码 | 全向/定向 | 清除/总数 | 清除率 | 总虚拟时间 秒 | 平均定位清除时间 秒/个 | 移动 米 | 检测次数 | 本地耗时 秒 |
|---|---|---|---|---|---|---|---|---|---|---|
| 3 | v4 | 8NV5-V67K-5J98-362W | 11/0 | 11/11 | 100% | 4772.590 | 433.872 | 18242.9 | 179 | 12.07 |

- 证据式退出：清除 11 个频道，另外 9 个频道由证书判定无源（`certified_absent`），覆盖与证书均完成，退出原因 `user_exit`。
- 尾段确认 0 秒；计时对账误差 2e-6 秒（与模拟器时间口径完全一致）；本地耗时 12.07 秒，远低于现实 20 分钟限制。
- 案例编码、请求 JSONL、certificate.json、summary.json 均保存于 `results_v4/`。
- 尝试继续演练问题4时，模拟器在启动瞬间出现"连接服务器超时"，随后界面停留在"服务器时间待校验"、开始按钮被禁用，未自动恢复；该局未产生任何机器狗请求（preflight 校验保护生效），不影响任何统计，也非算法问题。待模拟器状态恢复（重连或重启应用）后可再演练问题4。
